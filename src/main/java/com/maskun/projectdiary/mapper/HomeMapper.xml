<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.maskun.projectdiary.mapper.HomeCalendarMapper">
    <select id="selectMonthMemoList" parameterType="java.util.Map" resultType="com.maskun.projectdiary.vo.domain.Memo">
        SELECT
            memo_no memoNo,
            memo_date memoDate,
            user_id userId,
            memo_content memoContent
        FROM memo
        WHERE
            user_id = #{user.userId} AND
            DATE_FORMAT(memo_date, '%Y-%M')  = #{yearMonth}
        ORDER BY memo_date
    </select>
</mapper>