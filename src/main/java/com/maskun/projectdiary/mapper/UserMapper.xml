<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.maskun.projectdiary.mapper.UserMapper">
    <select id="findUserForLogin" parameterType="com.maskun.projectdiary.vo.domain.User" resultType="com.maskun.projectdiary.vo.domain.User">
        select
            user_id userId,
            user_level userLevel,
            createdate
        from user
        where user_id = #{userId} AND uesr_pw = PASSWORD(#{userPw})
    </select>
    <insert id="insertUser" parameterType="com.maskun.projectdiary.vo.domain.User">
        INSERT INTO user(user_id, uesr_pw)
        VALUES (#{userId}, PASSWORD(#{userPw}))
    </insert>
</mapper>