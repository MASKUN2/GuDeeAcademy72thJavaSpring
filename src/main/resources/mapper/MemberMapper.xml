<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.maskun.projectdiary.mapper.MemberMapper">
    <select id="getMemberForLogin" parameterType="com.maskun.projectdiary.vo.Member" resultType="com.maskun.projectdiary.vo.Member">
        select
            member_no memberNo,
            member_id memberId,
            member_level memberLevel,
            createdate
        from member
        where member_id = #{memberId} AND
              member_pw = PASSWORD(#{memberPw})
    </select>
    <insert id="insertMember" parameterType="com.maskun.projectdiary.vo.Member">
        INSERT INTO member
            (member_id, member_pw)
        values
            (#{memberId}, PASSWORD(#{memberPw}))
    </insert>
</mapper>